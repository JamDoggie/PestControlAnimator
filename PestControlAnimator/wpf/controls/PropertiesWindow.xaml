<UserControl x:Class="PestControlAnimator.wpf.controls.PropertiesWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PestControlAnimator.wpf.controls"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="500">
    <Grid Background="#2f2f38">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Canvas Background="#282830">

        </Canvas>

        <TabControl Grid.Row="1" Background="#2f2f38" BorderThickness="0">
            <TabItem Header="Sprite Properties">
                <Canvas Background="#2f2f38" Loaded="Canvas_Loaded" SizeChanged="Canvas_SizeChanged">

                </Canvas>
            </TabItem>

            <TabItem Header="Project Properties">
                <Grid Background="#292930">

                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
